name: app
description: Mahjong compass app.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.20.1
  freezed_annotation: ^2.2.0
  go_router: ^13.2.0
  hooks_riverpod: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1
  freezed: ^2.2.1
  build_runner: ^2.3.2
  custom_lint:
  riverpod_lint:

flutter:
  uses-material-design: true
